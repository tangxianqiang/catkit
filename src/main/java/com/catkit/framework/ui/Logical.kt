package com.catkit.framework.ui

/**
 * Only implements this interface can execute transaction-logic. It means that our "P"(Presenter that
 * acted P in MVP model) only worked for this interface.
 */
interface Logical{

}